/* eslint-disable import/no-anonymous-default-export */
import { routeConstants } from "shared/constants";
import Hoje from ".";

export default {
    path: routeConstants.HOJE.route,
    component: <Hoje/>
}